#!/bin/sh
# Gradle wrapper stub — Android Studio will regenerate this on first sync.
# To bootstrap manually: gradle wrapper --gradle-version 8.11.1
echo "Run 'gradle wrapper --gradle-version 8.11.1' to generate the full wrapper, or open in Android Studio."
exit 1
